services:
  - type: web
    name: leveraged-dca-backend
    env: docker
    dockerfilePath: ./apps/backend/Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
    plan: free
